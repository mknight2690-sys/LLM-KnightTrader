You are helping me set up LLM KnightTrader — an autonomous BloFin futures trading bot with a live dashboard.

REPO (clone this first):
https://github.com/1bananaonthewall-ux/llm-knighttrader

CANONICAL INSTRUCTIONS — read these files in the repo before doing anything:
1. ReadMe.tx   (full runbook — read entire file)
2. AGENTS.md   (short agent checklist)

Your job: install LLM KnightTrader, prove it runs end-to-end once, then STOP it and
hand off daily control to the user's desktop shortcuts. Do NOT leave the stack
running when you finish.

---

STEP 1 — ASK ME THESE QUESTIONS (do not skip)

A) API CREDENTIALS (paste in chat OR point to a file)
   Ask me:
   "Paste your BloFin API Key, Secret Key, and Passphrase here in chat.
    Also paste your OpenRouter API key (sk-or-...).
    If you prefer, give the full path to a file on disk instead."
   Then:
   - If I paste in chat: write BloFin keys to credentials/blofin.txt and OpenRouter to .env
   - If I give a file path: read it and copy into credentials/blofin.txt and .env
   - NEVER commit secrets to git or push to GitHub
   - Private agent chat is OK for keys during setup — do not echo keys back unnecessarily

B) BLOFIN + VPN / LOCATION
   Ask me: "What country are you physically in?"
   Ask me: "Do you use ProtonVPN? If yes, which country/server is your VPN exit?"
   BloFin blocks many regions. Official list:
   https://blofin.com/en/support/Terms/7296238795279-TERMS-OF-USE

   ProtonVPN + BloFin quick guide:
   - OK for BloFin exit: Netherlands, Switzerland, Poland, Romania, Norway, Japan (Free tier)
   - NOT for BloFin exit: United States, Canada, Singapore, Mexico
   - Plus plan: use EU countries not on BloFin's restricted list (e.g. Germany, Netherlands)
   VPN must stay connected to an allowed exit while the bot runs.

C) PYTHON
   Ask if Python 3.12+ is installed, or help me install it.
   Set KNIGHTTRADER_PYTHON if needed.

---

STEP 2 — SET UP (follow ReadMe.tx)

  git clone https://github.com/1bananaonthewall-ux/llm-knighttrader.git
  cd llm-knighttrader
  python -m venv .venv
  .venv\Scripts\activate
  pip install -r requirements.txt
  copy credentials\blofin.example.txt credentials\blofin.txt
  copy .env.example .env
  (fill credentials/blofin.txt and .env from my files — see Step 1A)

  Start (verification run only — you will stop it in Step 4):
  launcher\Start LLM KnightTrader.bat
  OR: python scripts\stack_launcher.py start --open-browser
  (creates "Start LLM KnightTrader" and "Stop LLM KnightTrader" on desktop)

  Dashboard: http://127.0.0.1:8765

---

STEP 3 — VERIFY END-TO-END (you must confirm ALL before stopping)

  Run these checks and report pass/fail to me:
  - GET http://127.0.0.1:8765/api/health → ok
  - GET http://127.0.0.1:8765/api/stack/status → trader status "online", count 1
  - GET http://127.0.0.1:8765/api/status → equity/available present (not broken)
  - Dashboard loads in browser; trader dot green; equity updating
  - Desktop has shortcuts: "Start LLM KnightTrader" and "Stop LLM KnightTrader"
  - If 403/geo errors: fix ProtonVPN exit country, restart once, re-verify

  Only proceed to Step 4 when every check passes.

---

STEP 4 — STOP AND HAND OFF (mandatory — do not skip)

  You MUST stop the stack before ending setup:
  launcher\Stop LLM KnightTrader.bat
  OR: python scripts\stack_launcher.py stop

  Confirm stopped:
  - python -c "from trader.stack_control import is_entire_stack_stopped; print(is_entire_stack_stopped())"
  - Must print True

  Then tell me clearly:
  "Setup is complete. I verified LLM KnightTrader end-to-end and stopped it.
   From now on, double-click Start LLM KnightTrader on your desktop to run.
   Double-click Stop LLM KnightTrader when you want to shut everything down.
   Start always kills any old instances and restarts dashboard + trader fresh."

  Do NOT leave trader or dashboard running in the background.
  Do NOT create scheduled tasks, startup entries, or monitor processes.

---

RULES FOR YOU (the agent)

- Read ReadMe.tx end-to-end before improvising.
- Ask for credentials in chat (paste keys) OR file path — write to credentials/blofin.txt and .env locally.
- Verify end-to-end (Step 3), then STOP (Step 4) — user starts via desktop only after that.
- Do not push API keys to GitHub.
- Confirm VPN exit country if I'm in a restricted region.
- If trader won't start: check data\logs\trader.err

Go. Start by asking me to paste my BloFin and OpenRouter keys in chat (or give a file path if I prefer).
